<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:SyncMe"
       xmlns:views="clr-namespace:SyncMe.Views"
       x:Class="SyncMe.AppShell">
    <!-- Display a bottom tab bar containing two tabs -->
    <TabBar>
        <ShellContent Title="Notes"
                      Icon="icon_feed.png"
                      ContentTemplate="{local:DIDataTemplate Type={x:Type views:NotesPage}}"
                      Route="notes"/>
        <ShellContent Title="Calendar"
                      Icon="icon_open_calendar.png"
                      ContentTemplate="{local:DIDataTemplate Type={x:Type views:CalendarPage}}"
                      Route="calendar"/>
        <ShellContent Title="New Event"
                      Icon="icon_create_event.png"
                      ContentTemplate="{local:DIDataTemplate Type={x:Type views:CreateEvent}}"
                      Route="newEvent"/>
    </TabBar>
</Shell>